fastlane_version "2.228.0"
opt_out_usage

desc "Build validation for CI"
lane :build_ci do
  sh("cd .. && cargo check --manifest-path gateway/Cargo.toml")
  sh("cd .. && docker compose build ai_service gateway")
end
